# Canonical Terms

Add preferred terms here.

Example:

- Use `research question`; avoid switching between `question`, `issue`, and `problem` when they mean the same thing.
- Use one English translation for each key local-language term.

