# Literature Registry

This folder is reserved for local generated literature-registry files.

Generated registry manifests and JSONL files can contain local paths or private literature metadata, so they are ignored by git by default. Keep only this README in the public template.
